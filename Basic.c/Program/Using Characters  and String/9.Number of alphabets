#include<stdio.h>
#include<string.h>
int main(){
    char c[100];
    scanf("%[^\n]",c);
    int count=0;
    for(int i=0;i<strlen(c);i++){
        if((c[i]>='a' && c[i]<='z') || (c[i]>='A' && c[i]<='Z')){
            count++;
        }
    }
    printf("%d",count);
}
